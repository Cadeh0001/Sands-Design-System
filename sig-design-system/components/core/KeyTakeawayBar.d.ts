/** @startingPoint section="SIG Primitives" subtitle="Navy bottom bar with orange label + one factual takeaway" viewport="700x120" */
export interface KeyTakeawayBarProps {
  /** "Key Takeaway" (default), "Read", "Key Distinction" */
  label?: string;
  children: React.ReactNode;
}
export declare function KeyTakeawayBar(props: KeyTakeawayBarProps): JSX.Element;
